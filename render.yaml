services:
  - type: web
    name: flask-app
    env: python
    region: oregon  # or frankfurt (choose closest to your users)
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    autoDeploy: true
    branch: main
